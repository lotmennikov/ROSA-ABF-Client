<LinearLayout  xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools" android:id="@+id/container"
        android:layout_width="match_parent" android:layout_height="match_parent"
        tools:context="hse.zhizh.abfclient.Activities.ProjectsActivity" tools:ignore="MergeRootFrame"
        android:orientation="vertical"
        android:descendantFocusability="blocksDescendants"
        android:clickable="true"
    android:background="@color/common_deepbackground">

    <ListView
        android:layout_width="match_parent"
        android:layout_height="fill_parent"
        android:id="@+id/projectsList"
        android:layout_margin="10dp"
        android:background="@color/common_frontbackground"
        android:descendantFocusability="blocksDescendants"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:text="No projects"
        android:id="@+id/projects_empty"
        android:layout_margin="10dp"
        android:background="#ffe5ffe7"
        android:padding="10dp" />
</LinearLayout>

